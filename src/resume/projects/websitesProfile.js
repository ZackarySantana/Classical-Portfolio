// Libraries
import React, { Fragment } from 'react';

// Components
import Profile from '../components/profile';

export default function WebsitesProfile(props) {
	return (
		<Fragment>
			{props.show &&
				<h1>hi</h1>
			}
		</Fragment>
	);
}
